<h1> Links </h1>
<%= flash[:goodbye] %>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title:<%= link.title %>
      URL:<%= link.url   %>
      Tags:<%= link.tags.map(&:tag_name).join(", ") %>
    </li>
  <% end %>
</ul>
